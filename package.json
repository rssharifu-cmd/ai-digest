{
  "name": "signal",
  "private": true,
  "version": "1.0.0",
  "description": "Signal — public/ static + api/ serverless (Vercel native layout)",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@vercel/node": "^3.2.29",
    "mongodb": "^6.5.0"
  }
}
